# ML Notebooks

Placeholder notebooks for dataset exploration, training analysis, and model evaluation.

These are work-in-progress and will be populated as the ML pipeline matures.
For actual training and evaluation, use `ml/src/train.py` and `ml/src/evaluate.py`.
